<Controls:MetroWindow x:Class="MODEXngine.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MODEXngine"
        xmlns:viewModels="clr-namespace:MODEXngine.ViewModels"
        mc:Ignorable="d"
        Title="MODEXngine" Height="450" Width="800">
    <Controls:MetroWindow.DataContext>
        <viewModels:MainWindowViewModel />
    </Controls:MetroWindow.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="MODEXngine" Style="{StaticResource Logo}" />
        
        <Controls:DropDownButton Grid.Row="1" ItemsSource="{Binding GameHeaders}">
            <Controls:DropDownButton.ItemTemplate>
                <DataTemplate>
                    <Label Content="{Binding GameName}" />
                </DataTemplate>
            </Controls:DropDownButton.ItemTemplate>
        </Controls:DropDownButton>

        <Button Grid.Row="2" Content="Start Game" />
    </Grid>
</Controls:MetroWindow>